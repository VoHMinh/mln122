# Use the Spring Boot API instead of the in-browser mock engine.
NEXT_PUBLIC_GAME_API_MODE=http

# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Vercel must use the public HTTPS URL of the deployed backend, for example:
# NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
